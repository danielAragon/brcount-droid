<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.betterride.brcount">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BRCount"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".viewcontrollers.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".viewcontrollers.activities.WelcomeActivity"
            android:label="@string/title_activity_welcome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".viewcontrollers.activities.LogInActivity"
            android:label="@string/title_activity_log_in"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".viewcontrollers.activities.CountingSessionsActivity"
            android:label="@string/title_activity_counting_sessions"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".viewcontrollers.activities.CountingActivity"
            android:label="@string/title_activity_counting"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".viewcontrollers.activities.ReportingActivity"
            android:label="@string/title_activity_reporting"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>